const db = require("../models");
const Utilisateur = db.utilisateur;
const Op = db.Sequelize.Op;

// Get likes for message_id
exports.getLikes = (req, res) => {};

// Set like from utilisateur_id for message_id
exports.setLike = (req, res) => {};
